<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Necessary due to Glide dependency -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".composestate.ComposeStateActivity"
            android:exported="false"
            android:label="@string/title_activity_compose_state"
            android:theme="@style/Theme.MyApplication" />

        <activity
            android:name=".ViewActivity"
            android:exported="false" />

        <activity
            android:name=".databinding.UserActivity"
            android:exported="false" />

        <activity
            android:name=".sunflower.GardenActivity"
            android:exported="false"
            android:theme="@style/Theme.Sunflower.NoActionBar" />

    </application>

</manifest>